declare namespace _default {
    const input: string;
    namespace output {
        const file: string;
        const format: string;
        const name: string;
        const sourcemap: boolean;
    }
    const plugins: any[];
}
export default _default;
